import React, { useState, useEffect} from 'react'

const Monitor = ({gridMonitor}) => {
    return (
        <div className="monitor">
            <div className="monitor__content">
                {
                    // gridMonitor && gridMonitor.content.map(item => (
                    //     <div className="monitor__item">
                    //         {item.item}
                    //         </div>
                    // ))
                    
                    // gridMonitor && gridMonitor.map(item => (
                    //     <div className="monitor__item">

                    //     </div>
                    // ))
                }
            </div>
        </div>
    )
}

export default Monitor
